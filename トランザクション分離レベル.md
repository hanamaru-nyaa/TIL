# トランザクション分離レベル

## 前提知識
### トランザクションとは
データベースに複数の処理が発生した時におかしくならないようにするための処理単位のこと。

トランザクション内の操作が「すべて実行される」か、「まったく実行されない」ことが保証できる。

### ACID特性とは
トランザクション処理に欠かせない特性

- 原子性（Atomicity）
- 一貫性（Consistency）
- 分離性（Isolation）
- 永続性（Durability）

今回扱うのは、ACID特性の中の分離性である。

分離性とは、複数トランザクションは互いに影響を与えないという特性。

同時に処理した時もごちゃごちゃにならずに分離されているように処理される。

## 分離レベルはなぜ必要？
トランザクションに完璧な分離性を持たせると、処理時間が長くなってしまうという欠点がある。

そこで、どれくらい厳密にトランザクションを分離するかを決めるのが分離レベルの役割になる。

## 分離レベルの種類
トランザクション分離レベルは以下の4つである。

[処理速度が遅い][安全]

- SERIALIZABLE
- REPEATABLE READ
- READ COMMITTED
- READ UNCOMMITTED

[処理速度が速い][危険]

### SERIALIZABLE
トランザクション実行時にトランザクションで読み取るすべての行に共有ロックをかける。

### REPEATABLE READ
最初に読み取った値をずっと参照する。

### READ COMMITTED
コミットされた変更は読み取れる。



MySQLのストレージエンジンのInnoDBデフォルトではREPEATABLE 

**ポスグレも調査する**

分離レベルごとに問題が発生するので表にしている。

〇：発生する　×：発生しない
|分離レベル|ファントムリード|ファジーリード|ダーティーリード|
|-|-|-|-|
|SERIALIZABLE|×|×|×|
|REPEATABLE READ|〇|×|×|
|READ COMMITTED|〇|〇|×|
|READ UNCOMMITTED|〇|〇|〇|

